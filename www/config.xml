<?xml version='1.0' encoding='utf-8'?>
<widget xmlns   = "http://www.w3.org/ns/widgets"
    xmlns:cdv   = "http://phonegap.com/ns/1.0"
    id="br.com.banggo.flexmath"
    versionCode = "10" 
    version     = "1.0">

    <!-- br.com.novelis.summit -->

    <!-- PHONEGAP 3.4.0 -->

    <name>FlexMath</name>
    <description>
        FlexMath
    </description>
    <author email="cesar.kamide@banggo.com.br" href="http://www.banggo.com.br/">
      Banggo -- Solucoes em Mobile
    </author>

    <content src="index.html" />

    <!-- FEATURE -->
    <feature name="http://api.phonegap.com/1.0/device" />
    <feature name="StatusBar">
      <param name="ios-package" value="CDVStatusBar" onload="true" />
    </feature>
    <feature name="Calendar">
        <param name="ios-package" value="Calendar" />
    </feature>

    <!-- PERMISSIONS -->
    <preference name="permissions" value="none"/>

    <!-- PPREFERENCES -->
    <!-- Customize your app and platform with the preference element. -->
    <preference name="phonegap-version"      value="3.4.0" />               <!-- all: current version of PhoneGap -->
    <preference name="orientation"                value="portrait" />       <!-- all: default means both landscape and portrait are enabled -->
    <preference name="target-device"              value="handset" />      <!-- all: possible values handset, tablet, or universal -->
    <preference name="fullscreen"                 value="true" />           <!-- all: hides the status bar at the top of the screen -->
    <preference name="webviewbounce"              value="false" />           <!-- ios: control whether the screen 'bounces' when scrolled beyond the top -->
    <preference name="prerendered-icon"           value="true" />           <!-- ios: if icon is prerendered, iOS will not apply it's gloss to the app's icon on the user's home screen -->
    <preference name="stay-in-webview"            value="false" />          <!-- ios: external links should open in the default browser, 'true' would use the webview the app lives in -->
    <preference name="ios-statusbarstyle"         value="black-opaque" />   <!-- ios: black-translucent will appear black because the PhoneGap webview doesn't go beneath the status bar -->
    <preference name="detect-data-types"          value="true" />           <!-- ios: controls whether data types (such as phone no. and dates) are automatically turned into links by the system -->
    <preference name="exit-on-suspend"            value="false" />          <!-- ios: if set to true, app will terminate when home button is pressed -->
    <preference name="show-splash-screen-spinner" value="false" />           <!-- ios: if set to false, the spinner won't appear on the splash screen during app loading -->
    <preference name="auto-hide-splash-screen"    value="true" />           <!-- ios: if set to false, the splash screen must be hidden using a JavaScript API -->
    <preference name="disable-cursor"             value="false" />          <!-- blackberry: prevents a mouse-icon/cursor from being displayed on the app -->
    <preference name="android-minSdkVersion"      value="7" />              <!-- android: MIN SDK version supported on the target device. MAX version is blank by default. -->
    <!-- android: app install location. 'auto' will choose.'internalOnly' is device memory. 'preferExternal' is SDCard. -->
    <preference name="android-installLocation"    value="auto" />

    <preference name="UIWebViewBounce" value="false" />
    <preference name="DisallowOverscroll" value="true" />

    <!-- Plugins can also be added here. -->
    <!--
        <gap:plugin name="Example" />
        A list of available plugins are available at https://build.phonegap.com/docs/plugins
    -->
    <cdv:plugin name="org.apache.cordova.device" />
    <cdv:plugin name="org.apache.cordova.console" />    
    <cdv:plugin name="com.phonegap.plugin.statusbar" />
    <cdv:plugin name="nl.x-services.plugins.calendar" />
    <cdv:plugin name="org.apache.cordova.inappbrowser" />

    <!-- ICONS -->
    <icon src="img/icon/icon_ios7.png" cdv:platform="ios" width="120" height="120" />
    <icon src="img/icon/icon_ios6.png" cdv:platform="ios" width="57" height="57" />    
    <cdv:splash src="img/splash/splash.png" cdv:platform="ios" width="640" height="1136" />
    <cdv:splash src="img/splash/splash2.png" cdv:platform="ios" width="640" height="960" />

    <!-- Define access to external domains. -->
    <access origin="*" />

</widget>
